const qs=(s,p=document)=>p.querySelector(s),qsa=(s,p=document)=>[...p.querySelectorAll(s)];const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches,coarse=matchMedia('(pointer: coarse)').matches;
const loader=qs('.loader'),count=qs('.loader-count'),line=qs('.loader-line i');if(!reduced){let n=0;const t=setInterval(()=>{n=Math.min(100,n+Math.ceil((100-n)*.1));count.textContent=String(n).padStart(3,'0');line.style.width=n+'%';if(n===100){clearInterval(t);setTimeout(()=>loader.classList.add('done'),180)}},38)}else loader.remove();
if(!coarse){const c=qs('.cursor'),l=qs('.cursor span');let x=innerWidth/2,y=innerHeight/2,cx=x,cy=y;addEventListener('pointermove',e=>{x=e.clientX;y=e.clientY});(function loop(){cx+=(x-cx)*.17;cy+=(y-cy)*.17;c.style.transform=`translate(${cx-33}px,${cy-33}px)`;requestAnimationFrame(loop)})();qsa('[data-cursor]').forEach(el=>{el.onmouseenter=()=>{l.textContent=el.dataset.cursor;c.classList.add('active')};el.onmouseleave=()=>{l.textContent='VIEW';c.classList.remove('active')}})}
const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');io.unobserve(e.target)}}),{threshold:.13});qsa('.reveal').forEach(e=>io.observe(e));
qsa('.magnetic').forEach(el=>{if(coarse)return;el.onpointermove=e=>{const r=el.getBoundingClientRect();el.style.transform=`translate(${(e.clientX-r.left-r.width/2)*.14}px,${(e.clientY-r.top-r.height/2)*.14}px)`};el.onpointerleave=()=>el.style.transform=''});
const menu=qs('.menu'),panel=qs('.panel');menu.onclick=()=>panel.classList.toggle('open');qsa('.panel a').forEach(a=>a.onclick=()=>panel.classList.remove('open'));
const modal=qs('.case-modal'),feature=qs('.feature');feature.onclick=()=>modal.showModal();feature.onkeydown=e=>{if(e.key==='Enter'||e.key===' ')modal.showModal()};qs('.close').onclick=()=>modal.close();modal.onclick=e=>{if(e.target===modal)modal.close()};
qsa('[data-copy]').forEach(b=>b.onclick=async()=>{try{await navigator.clipboard.writeText(b.dataset.copy)}catch{}const t=qs('.toast');t.textContent=b.dataset.copy;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),2600)});
